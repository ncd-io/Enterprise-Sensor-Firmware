There are two firmware versions, depending on the hardware ID. The hardware ID is included in the  FLY payload. if the hardware ID does NOT match firmware will not update. 
default setting 

Interval -- 1200 sec 
Boot time -- 12sec 
auto chekc -- disable 
